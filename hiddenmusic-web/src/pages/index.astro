---
import Layout from '../layouts/Layout.astro';
import VaultTerminalScene from '../components/terminal/VaultTerminalScene';
import { fetchAlbums } from '../lib/api';

const albums = await fetchAlbums();
---

<Layout title="Hidden Music Vault | The Underground Music Terminal">
  <VaultTerminalScene client:load initialAlbums={albums} />
</Layout>
